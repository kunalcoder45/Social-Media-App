<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
        integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
    <div class="bg-zinc-900 w-full min-h-screen p-2">
        <div class="max-w-2xl mx-auto border border-zinc-800 p-4 rounded-md">
            <div class="flex justify-between items-center">
                <h1 class="text-3xl text-white">Hello, <%= user.name %> 👋🏻</h1>
                <a href="/logout" class="text-red-500 cursor-pointer">
                    <i class="fa-solid fa-arrow-right-from-bracket"></i>
                </a>
            </div>
            <h4 class="text-slate-300 mt-2">You can edit a post..</h4>

            <!-- Update a post form -->
            <form action="/update/<%= post.id  %>" method="post" class="mt-4 border-b border-zinc-800 pb-4">
                <textarea name="content"
                    class="w-full h-auto overflow-auto bg-zinc-800 text-white p-4 rounded-md resize-y"
                    placeholder="Edit your post..."><%= post.content %></textarea>
                <input type="submit" class="bg-yellow-500 text-black px-4 py-2 rounded-full mt-2 cursor-pointer"
                value="Update Post">
            </form>
        </div>
    </div>
</body>

</html>